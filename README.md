# neospec
A self-contained test runner and coverage tool for Neovim plugins and distributions. Written in Go, neospec manages its own Neovim binary — no system install required. Supports LCOV, Cobertura, Coveralls, and console coverage reports, dynamic README badge updates, and ships as both a CLI tool and a GitHub Action.
